#include<bits/stdc++.h>
using namespace std;

class Solution {
public:
    bool uniformArray(vector<int>& nums1) {
        // case 1: if all even then return true;
        // case 2: if atleast one odd. And even - odd = odd, we can convert all evens to odds therfore reaching out goal
       return true;
    }
};