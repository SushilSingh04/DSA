from typing import List, 
class Solution:
    ans = []
    def isValid(r:int, c:int, num:str, sudoku: List[List[str]]):

    def solver(r, c, emptyCell, sudoku):
        if emptyCell == 0:
            ans = sudoku
            return
        
        for char 

    def solveSudoku(board):
        pass